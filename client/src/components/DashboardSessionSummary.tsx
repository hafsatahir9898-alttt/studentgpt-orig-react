type StudySession = { id: number; startedAt: Date | string; minutesStudied: number };

export function DashboardSessionSummary({ minutesToday, sessions }: { minutesToday: number; sessions: StudySession[] }) {
  return <section className="mx-auto mt-5 max-w-[1200px] px-5 pb-8 sm:px-8 lg:px-10"><div className="rounded-2xl border border-[#A070A1] bg-[#FDF3F3] p-5 sm:p-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-[16px] font-semibold tracking-[-.02em]">Study sessions</h2><p className="mt-1 text-[13px] text-[#724060]">Logged time from your actual completed study sessions.</p></div><p className="text-[24px] font-semibold tracking-[-.04em] text-[#A070A1]">{minutesToday} <span className="text-[12px] font-medium text-[#724060]">minutes today</span></p></div>{sessions.length ? <div className="mt-5 divide-y divide-[#A070A1]">{sessions.slice(0, 4).map(session => <div key={session.id} className="flex items-center justify-between py-3 text-[13px]"><span>{new Date(session.startedAt).toLocaleDateString(undefined, { weekday: "short", month: "short", day: "numeric" })}</span><span className="font-medium text-[#724060]">{session.minutesStudied} minutes</span></div>)}</div> : <p className="mt-5 rounded-xl bg-[#F8E7E7] p-4 text-[13px] text-[#724060]">Log a session from the study planner to see your focused time here.</p>}</div></section>;
}
